Add workspace collaboration (shared folders, team roles, comments), especially since enterprise messaging already implies it.
Add document version history and “regenerate from version”.
Add richer search/filtering (full-text, template type, date range, status, tags).
Add template editor UI with schema builder and preview.